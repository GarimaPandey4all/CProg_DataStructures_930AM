Red black Tree: Balanced Height Binary Serach Tree

1. If tree is empty, create new node as root node with color black.

2. If tree is not empty, create new node as leaf node with color red.

3. If parent of new node is black then exit.

4. If parent of new node is red, then check the color of parent's sibling of newnode.
	a.) If color is black or null then do suitable rotation or recolor

	b.) If color is red then recolor and also check if parent's parent of newnode is not root node then recolor it & recheck.
	
-root and leaf = Black
-no two adjacent red nodes
-count no of black nodes on each path

Question: 30, 25, 40, 60, 2, 1, 70

